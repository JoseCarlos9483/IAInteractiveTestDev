namespace Models.Enum
{
    /// <summary>
    /// Enum que se basa en la tabla de Status Sales
    /// </summary>
    public enum StatusSaleEnum
    {
        Pending = 1,
        InProcess = 2,
        Completed = 3,
        Delivered = 4,
        Canceled = 5
    }
}
